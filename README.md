# Graph-Algorithms-in-Python
BFS, DFS, UCS, DLS, and IDS are fundamental uninformed search algorithms (also known as blind search) in Artificial Intelligence. They systematically explore a search space (a graph or tree) to find a solution without having prior domain knowledge about the goal's location
